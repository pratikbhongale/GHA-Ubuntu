name: Environment variable
on: workflow_dispatch
env:
 APP_ENV: "dev"   #workflow level
jobs:
 env_demo:
  env:
   APP_ENV: "test"  #job level
  runs-on: ubuntu-latest
  steps:
   - name: Connecting to Env 
     env:
      APP_ENV: "prod"  #job level
     run: echo "App is connecting to $APP_ENV"
